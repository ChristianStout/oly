{
    "version": "2.0.0",
    "command": "",
    "args": [],
    "tasks": [
        {
            "label": "clean_and_makedir",
            "type": "shell",
            "command": "rm -rf build/ && mkdir -p build",
        },
        {
            "label": "build",
            "type": "shell",
            "command": "odin build src -debug -out:build/debug.bin",
            "group": "build"
        },
        {
            "label": "Build",
            "dependsOn": [
                "clean_and_makedir",
                "build"
            ]
        }
    ]
}